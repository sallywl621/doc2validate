# Notebooks

This directory contains exploratory analysis notebooks used during corpus construction, downloaded artifact auditing, and pilot execution analysis.

These notebooks are preserved for provenance and analysis history. They are not the primary pipeline entry points.

For reproducible pipeline runs, use the Python modules under `src/` and the shell/Python entry points under `scripts/`.
